using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.Events;

public class UI_CuttingProgress : MonoBehaviour
{
    /*
    CuttingBoardScript cuttingBoardScript;
    private float _externalTimer;
    [SerializeField] private float _timerIndicator;
    [SerializeField] private float _timerMaxIndicator;
    [SerializeField] private Image _radialIndicator = null;

    void Awake()
    {
    }

    // Update is called once per frame
    void Update()
    {
        _externalTimer = cuttingBoardScript.timer;

        if(_externalTimer > 0)
        {
            
            _radialIndicator.enabled = true;
            _radialIndicator.fillAmount = _externalTimer;

            if (_timerIndicator <= 0)
            {
                _timerIndicator = _timerMaxIndicator;
                _radialIndicator.fillAmount = _timerMaxIndicator;
                _radialIndicator.enabled = false;
            }

        }
    }
    */
}
